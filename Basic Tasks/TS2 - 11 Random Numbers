''' to print three random integers between the given numbers'''

from random import randint

def ran(n1,n2):
    return randint(n1, n2)


x1 = int(input("Enter the first number:"))
x2=int(input("Enter the second number:"))

for i in range(0,3):
    
    print(ran(x1,x2))
    
